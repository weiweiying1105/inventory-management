{
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "scripts": {
    "dev": "concurrently --raw \"npm run dev --prefix next-server\" \"npm run dev --prefix next-client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1"
  }
}
